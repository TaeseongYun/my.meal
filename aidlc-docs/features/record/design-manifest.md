# Design Manifest — 기록 생성/수정 화면

Figma `T3hswISeWAzU2nD6hESZJL` node `832:98315` ("생성하기"), 추출 2026-09-03.
REST nodes API가 세션 내내 429 — images 렌더(2x PNG)로 레이아웃·색을 픽셀 실측 판독.
manifest.json/tokens.json은 API 복구 시 추출해 커밋 예정 (kmp-figma-to-code 규약).

## 레이아웃 (360x760, bg = 토큰 bg1 #FFFFFF)
- 상단바 44dp: ← 뒤로가기 24dp (#53422C) / 우측 "저장" (h3, #53422C)
- 날짜·시간 행: "2023년 6월 4일 금요일" (h3, #968F81→text3 정규화) + 시간 칩 "오후 12:53 ›"
  (bg3 r8, body1 text4, 패딩 12x8, 셰브런 5x9 #53422C)
- 끼니 칩 4개: ☀️아침 / ⛅점심 / 🌑저녁 / 🍰간식 — 각 74dp(weight 1f)·높이 32dp·r8·간격 8dp.
  선택 = #413A31 bg + 흰 텍스트, 비선택 = bg3 + text3 (실측 #B1AFA9→text3 정규화). 기본 선택 = 아침
- 도시락 프레임 245x262 r24 #413A31, 내부 r16 보더 좌우·상 8dp / 하 24dp(받침).
  내부 = 일러스트 자리표시자(토마토·브로콜리·주먹밥) 또는 사진(Crop).
  카메라 버튼 44dp r8 #B6B2AC + 흰 카메라 아이콘, 내부 우하단 16dp(s16) 오프셋
- 음식 이모지 행: 🍚🍜🥗🍖🍞🍔🍣🍰 (20sp) + "?" 타일(22dp r3 #DBD6CA α0.52 + 흰 "?") — SpaceBetween, 단일 선택
- 체크박스 행: 22dp r6 (미체크 = 1.5dp text2 보더 / 체크 = #413A31 채움 + 흰 체크) + "대표 게시물 설정하기" (body1, text3)
- 메모 영역: bg3 r8 높이 240dp(하단 절단 디자인 → 고정 높이 채택), 괘선 첫줄 41dp·피치 28dp (#000 α0.06),
  BasicTextField body1 lineHeight 28, 플레이스홀더 "메모를 남겨보세요" (실측 #BDB8AC→text3 정규화)
- 세로 간격: 상단바→날짜 24 / 날짜→칩 20 / 칩→프레임 40(실측 54 → s40 정규화) / 프레임→이모지 40 /
  이모지→체크 16 / 체크→메모 16. 콘텐츠 좌우 여백 실측 20 → s16 정규화 (home 동일)
- 화면 세로 초과분은 상단바 제외 전체 스크롤

## 토큰 외 값 (디자이너 확인 항목)
| 값 | 위치 | 처리 |
|----|------|------|
| #413A31 (RecordDark) | 선택 칩·도시락 프레임 | feature 로컬 상수 — home HomeDark와 동일 실측, 다크 서피스 토큰 승격 후보 |
| #53422C (AccentBrown) | 뒤로가기·저장·시간 셰브런 | feature 로컬 상수 (home DayBrown과 동일 실측) |
| #B6B2AC (CameraGray) | 카메라 버튼 면 | feature 로컬 상수 (렌더 실측) |
| #DBD6CA α0.52 | "?" 타일 | feature 로컬 상수 (home 미기록 박스와 동일) |
| #000000 α0.06 | 메모 괘선 | feature 로컬 상수 (home 글귀 괘선과 동일) |
| #968F81 → text3 / #B1AFA9 → text3 / #BDB8AC → text3 | 날짜·비선택 칩·플레이스홀더 | 토큰 정규화 — 스타일 가이드 우선 |
| 프레임 좌우 비대칭 (실측 좌 66 / 우 48.5) | 도시락 프레임 | 가운데 정렬 채택 — 수기 일러스트 오프셋으로 판단 |

## 확정 필요 항목 (스텁 결정)
- **날짜 요일 불일치**: 디자인 "2023년 6월 4일 금요일" — 실제 6/4는 일요일. 날짜에서 요일 파생으로 구현(테스트 고정)
- **간식 슬롯**: 요구사항 언급은 아침/점심/저녁 3개였으나 디자인엔 🍰간식 존재 — 디자인 기준 4개 채택 (UI 전용 enum, 데이터 슬롯 모델링은 F-2 결정)
- 음식 이모지 선택 표시: 디자인에 선택 상태 없음 — bg4 배경 채택
- 음식 이모지 9종의 의미(카테고리 vs 자유 선택)·저장 스키마 매핑: F-2에서 확정
- "대표 게시물 설정하기"의 대상(홈 캘린더 마크?)·동작: F-2/F-5에서 확정
- 저장·카메라·시간 변경: 이번 슬라이스 no-op — F-2 (사진 선택 expect/actual, 시간 피커, upsert)

## 에셋 비고
- `lunchbox_placeholder.webp` (451x452, 2.9KB): 2x 렌더에서 프레임 내부 크롭 — 베이크된 카메라 버튼은
  종이색(#FAF8F4)으로 패치 후 Compose 오버레이로 대체. `TODO(렌더 크롭)`: 노드 export 확보 시 원본 교체
- 아이콘 4종(←, ›, 카메라, 체크) 전부 Canvas 근사 — images API 429로 렌더 크롭 불가, 확보 시 교체 후보
- 끼니·음식 이모지 = 텍스트 렌더 (에셋 0개). 디자인은 애플 이모지 래스터 — Android는 Noto로 렌더 (home 동일 수용)
