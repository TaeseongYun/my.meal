# Design Manifest — 로그인 화면

Figma `T3hswISeWAzU2nD6hESZJL` node `832:48657` (섹션 C `832:26876` 내), 추출 2026-08-31.

## 레이아웃 (360x760, bg = 토큰 bg3 #F8F5ED)
- 타이틀: "매일 먹는 도시락,\n간편하게 기록하세요!" — LeeSeoyun 32/38 (토큰 스케일 밖 → h1 파생)
- 서브타이틀: "식도록과 함께하는 도시락 일기" — 16/26 (h4 파생)
- 히어로 일러스트(주먹밥·문어 캐릭터 도시락) + 스피드라인 — 벡터 수천 개 → 2x 렌더 크롭 `login_hero.png`로 번들
- 카카오 버튼: 320x48 r4, #FFE617, 심볼(비트맵 크롭 `ic_kakao.png`), "카카오로 로그인"
- 이메일 버튼: 320x48 r4, #222222, 흰 메일 아이콘(`ic_mail_white.png`), "이메일로 로그인"

## 토큰 외 값 (디자이너 확인 항목)
| 값 | 위치 | 처리 |
|----|------|------|
| #392F21 (타이틀 색) | 텍스트 | 토큰 text4(#3C3025)로 정규화 — 스타일 가이드 우선 |
| #FFE617 / #444444 | 카카오 버튼 | 브랜드 색 — feature 로컬 상수 (DS 토큰 승격 안 함) |
| #222222 | 이메일 버튼 배경 | feature 로컬 상수 — 다크 서피스 토큰 필요 여부 디자이너 확인 |
| 32/38, 16/26 | 타이포 | 토큰 파생(copy) — 디스플레이 스케일 추가 여부 확인 |
| stroke #00CC8F (이메일 버튼) | JSON에만 존재, 렌더에 미표시 | 미적용 (렌더 기준) |

## 에셋 비고
- 크롭 에셋 3종은 배경색이 각자의 서피스 색과 동일한 상태로 잘림 (투명 아님) — 배경 토큰 변경 시 재추출 필요
- 2x 해상도 (Figma API 3x는 rate limit — 필요 시 재추출)
